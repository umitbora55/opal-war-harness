name: OPAL WAR HARNESS

on:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: '0 3 * * 1'

jobs:
  constitution:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: opal-war-harness
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npm run build
      - run: npm run certification:validate
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: war-harness-constitution
          path: |
            opal-war-harness/reports/certification/**
  smoke:
    needs: constitution
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: opal-war-harness
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npm run build
      - run: npm run smoke -- --config ./configs/base.json
      - run: npm run certification:gate -- --gate pr-gate
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: war-harness-smoke-report
          path: |
            opal-war-harness/reports/**
  behavioral:
    needs: constitution
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: opal-war-harness
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npm run behavioral -- --config ./configs/sandbox.json
      - run: npm run certification:gate -- --gate nightly-gate
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: war-harness-behavioral-report
          path: |
            opal-war-harness/reports/**
  certification:
    needs: constitution
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: opal-war-harness
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npm run certify -- --config ./configs/release.json
      - run: npm run certification:gate -- --gate pre-release-cert
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: war-harness-certification-report
          path: |
            opal-war-harness/reports/**
